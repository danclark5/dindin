<% provide :title, "Feedback" %>
<h2 class="subtitle is-2"> Have something you want to see?</h2>
<p>
  Have an idea how we can make this better? Just want to tell us how you use DnnrPckr? Drop us a note!
</p>

<br/>

<%= form_with(url: :feedback, method: "post") do %>
  <div class="field">
    <label class="label">Feedback</label>
    <div class="control">
      <%= text_area_tag(:feedback, nil, class: "textarea")%>
    </div>
  </div>
  <div class="field">
    <div class="control">
      <%= submit_tag("Send", class: "button is-primary") %>
    </div>
  </div>
<% end %>
