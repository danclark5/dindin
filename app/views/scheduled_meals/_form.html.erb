<%= form_with(model: scheduled_meal, local: true) do |form| %>
  <% if scheduled_meal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(scheduled_meal.errors.count, "error") %> prohibited this scheduled_meal from being saved:</h2>

      <ul>
      <% scheduled_meal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :meal_id %>
    <%= form.text_field :meal_id %>
  </div>

  <div class="field">
    <%= form.label :date %>
    <%= form.date_select :date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
